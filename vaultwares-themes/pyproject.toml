[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vaultwares-themes"
version = "1.0.0"
description = "VaultWares design system — theme tokens, color palette, and Qt exporter"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["vaultwares_themes"]
